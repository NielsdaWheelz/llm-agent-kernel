[project]
name = "llm-agent-kernel"
version = "0.1.0"
description = "Bounded orchestration kernel for tool-using language-model agents"
readme = "README.md"
requires-python = ">=3.12"
license = "MIT"
license-files = ["LICENSE"]
authors = [
    { name = "Niels-Erik Nandal", email = "nielseriknandal@gmail.com" },
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
    "Typing :: Typed",
]
dependencies = [
    "llm-tools @ git+https://github.com/NielsdaWheelz/llm-tools.git@728f35c0b3a8be91b380ed4258d2b73ad68fc8fa",
    "provider-runtime[codex-sdk] @ git+https://github.com/NielsdaWheelz/llm-calling.git@a5d9c8e0c1c851daee0731554e0a4a326d3c2819",
    "pydantic>=2.11,<3",
]

[project.urls]
Repository = "https://github.com/NielsdaWheelz/llm-agent-kernel"
Issues = "https://github.com/NielsdaWheelz/llm-agent-kernel/issues"
Security = "https://github.com/NielsdaWheelz/llm-agent-kernel/security/policy"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/llm_agent_kernel"]

[tool.hatch.metadata]
allow-direct-references = true

[dependency-groups]
dev = [
    "build>=1.2.2",
    "pip-audit>=2.9.0",
    "pyright>=1.1.390",
    "pytest>=8.3.0",
    "pytest-asyncio>=0.24.0",
    "pytest-socket>=0.8.0",
    "ruff>=0.8.0",
]

[tool.ruff]
target-version = "py312"
line-length = 100
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "W", "F", "I", "B", "UP"]
ignore = ["E501"]

[tool.ruff.format]
quote-style = "double"

[tool.pyright]
typeCheckingMode = "standard"
pythonVersion = "3.12"
include = ["src", "tests", "examples"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_functions = ["test_*"]
addopts = "-v --tb=short --strict-markers -m 'not live' --disable-socket --allow-unix-socket"
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
markers = [
    "live: opt-in provider qualification requiring an external account",
]
